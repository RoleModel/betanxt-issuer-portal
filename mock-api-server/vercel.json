{
  "installCommand": "cd .. && npm_config_verify_dependencies_policy_before_install=false pnpm install --frozen-lockfile",
  "buildCommand": "turbo run build --filter=mock-api-server",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "ignoreCommand": "npx turbo-ignore",
  "crons": [
    {
      "path": "/api/cron/tabulation-distribute",
      "schedule": "0 8 * * *"
    }
  ]
}

{
  "installCommand": "cd .. && npm_config_minimum_release_age=0 pnpm install --frozen-lockfile",
  "buildCommand": "turbo run build --filter=mock-api-server",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "ignoreCommand": "npx turbo-ignore",
  "crons": [
    {
      "path": "/api/cron/tabulation-distribute",
      "schedule": "0 8 * * *"
    }
  ]
}
